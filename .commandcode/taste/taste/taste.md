# Taste
- Communicates requirements as a detailed, structured Detailed Requirement Document (DRD) with numbered sections (project overview, objectives, scope, functional & non-functional requirements, tech stack, architecture, constraints, deliverables, success metrics) rather than ad-hoc instructions. Confidence: 0.7
- Prioritizes budget-friendly, cost-effective solutions: explicitly favors open-source tools and free cloud tiers (e.g., Heroku/Vercel free tier, minikube for dev). Confidence: 0.8
- Treats security and compliance (OAuth2 authentication, GDPR for user data) as first-class, non-negotiable requirements. Confidence: 0.5
- Runs multiple concurrent workstreams (practice exercise series alongside full application builds) and redirects the assistant between them by project name, expecting it to remember and resume the prior state of that work. Gives terse, context-dependent prompts like "resume the prior work" / "resume the jobsearch agents work" and expects the assistant to pick up exactly where it left off (including queued/pending items), and often asks for a completion-status check ("is it completed") so the assistant should verify and report state rather than redo work. Confidence: 0.8
- Wants the project executed/verified before it is pushed to the remote ("lets execute the project then we can push"); the assistant should run the app (dev servers + end-to-end smoke test of the flow) and confirm it works before pushing to origin. Confidence: 0.6
- Wants every feature practically testable: asks for clear step-by-step instructions on how to test each component (e.g., each agent), and expects the UI to be built out so features can be exercised interactively from the UI rather than only via API/CLI ("you make sure to build the UI as well for required agents"). Confidence: 0.8
- Expects the assistant to verify factual claims (file/folder existence, exact paths) against the actual filesystem and git before asserting them, and directly calls out perceived inaccuracies ("test folder is not availabe ..why are telling wrong") instead of letting them pass. Confidence: 0.6
